from setuptools import setup

setup(
    name='handler',
    url='https://github.com/LeonardoRez/fss-feature-hndlr',
    author='Leonardo Rezende',
    author_email='leonardorec1@gmail.com',
    packages=['features_handler'],
    install_requires=['numpy'],
    version='0.1',
    # license='MIT',
    description='Functions for handle the features extracted by Rafael Lima on Identification system for smart homes using footstep sounds',
    # We will also need a readme eventually (there will be a warning)
    # long_description=open('README.txt').read(),
)
